#include <stdio.h>

void main()
{
    // unlike the printf, the puts adds a newline after printing the values
    puts("Hello there! How are you doing?"); // put -> PUT String

    // no need for return statements since the function returns nothing
    // the puts function receives only one argument.

    // so something like this raise an error
    // puts("%d", 34)

    // if you need to work with formatted strings or need more control on your
    // print statements, the printf function is best.
}
