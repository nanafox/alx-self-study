#include <stdio.h>

/**
 * main - bitwise operations
 * Return: 0
*/
int main(void)
{
	int x = 4, y = 8;

	printf("x & y: %d\n", x & y);
	printf("x | y: %d\n", x | y);
	printf("x ^ y: %d\n", x ^ y);

	return (0);
}
