/*
Write a program that can print out the numeric value of the newline character (\n).
*/

#include <stdio.h>

int main(void)
{
    char newline = '\n';
    printf("Number value of newline character (\\n): %d\n", newline);

    return 0;
}