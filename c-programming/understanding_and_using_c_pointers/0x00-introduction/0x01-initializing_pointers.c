#include <stdio.h>

/**
 * main - initializing pointers
 *
 * Return: 0
 */
int main(void)
{
	int num;
	int *ptr_to_num;

	num = 67;
	ptr_to_num = &num; /* store the address of the num variable */

	return (0);
}
