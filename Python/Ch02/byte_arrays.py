# filling with zeros
zero_filled = bytearray(5)
print(zero_filled)

# range of integers
nums = bytearray(range(1,6))
print(nums)

# string literal
name = bytearray(b'Maxwell')
print(name)
print(name.upper()) # allows for string methods
