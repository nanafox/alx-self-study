#!/usr/bin/python3

"""
Using the `eval()` built-in function to run single line codes
"""
if __name__ == "__main__":
    x = 1
    y = 2
    result = eval('x + y')

    print(f"{x} + {y} = {result}")

a = 10
